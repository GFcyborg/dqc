ToDo:

create unit tests
app to parse a simple(=no include) example
handle include
inject openqasm teleportations in a specific line
find eligible split points (before a loop?)
split original/monolithic code into chunks
...